specVersion: "v1beta"
serviceId: "product-feedback-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "product-feedback-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
